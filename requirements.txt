# Build dependencies for the dashboard and the GitHub Pages deploy.
dashdown-md[mistral]   # Dashdown CLI + Mistral provider for the <Ask /> component
gdown                  # fetches the Datablist sample CSVs from Google Drive
